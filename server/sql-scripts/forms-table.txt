DROP TABLE forms;

CREATE TABLE forms (
    id SERIAL PRIMARY KEY,
    consultant_id INTEGER,
    customer VARCHAR(50),
    sales_id INTEGER,
    manager_id INTEGER,
    date DATE,
    created_at TIMESTAMP DEFAULT current_timestamp,
    updated_at TIMESTAMP DEFAULT current_timestamp
);

CREATE FUNCTION update_timestamp()
RETURNS TRIGGER AS '
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END' LANGUAGE 'plpgsql';

CREATE TRIGGER update_trigger
BEFORE UPDATE ON forms
FOR EACH ROW
EXECUTE PROCEDURE update_timestamp();

INSERT INTO forms (consultant_id, customer, sales_id, manager_id, date) VALUES (7, 'Ica', 2, 3, '2023-08-13');
INSERT INTO forms (consultant_id, customer, sales_id, manager_id, date) VALUES (13, 'Nocco', 1, 4, '2023-08-23');
INSERT INTO forms (consultant_id, customer, sales_id, manager_id, date) VALUES (22, 'Levis', 1, 7, '2023-09-03');
INSERT INTO forms (consultant_id, customer, sales_id, manager_id, date) VALUES (19, 'Dell', 3, 7, '2023-09-13');

SELECT * FROM forms;
