CREATE TABLE forms (
    id SERIAL PRIMARY KEY,
    consultant VARCHAR(70),
    customer VARCHAR(70),
    sales VARCHAR(70),
    date DATE,
    created_at TIMESTAMP DEFAULT current_timestamp,
    updated_at TIMESTAMP DEFAULT current_timestamp
);

CREATE FUNCTION update_timestamp()
RETURNS TRIGGER AS '
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END' LANGUAGE 'plpgsql';

CREATE TRIGGER update_trigger
BEFORE UPDATE ON forms
FOR EACH ROW
EXECUTE PROCEDURE update_timestamp();

INSERT INTO forms (consultant, customer, sales, date) VALUES ('Agnes', 'Ica', 'Sebbe', '2023-08-13');
INSERT INTO forms (consultant, customer, sales, date) VALUES ('Lucas', 'Nocco', 'Sebbe', '2023-08-23');
INSERT INTO forms (consultant, customer, sales, date) VALUES ('Wilma', 'Levis', 'Sebbe', '2023-09-03');
INSERT INTO forms (consultant, customer, sales, date) VALUES ('Emelie', 'Dell', 'Sebbe', '2023-09-13');
